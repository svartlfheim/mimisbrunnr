// import {inMemoryClient, Client} from 'api-client'

// let client: Client = inMemoryClient

// if (process.env.NODE_ENV !== "development") {
//     // here we'd switch to fetch based client
// }

// export default client;
export {}